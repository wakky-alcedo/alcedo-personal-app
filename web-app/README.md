# Alcedo Web App

Minimal React + Vite frontend to view/create tasks against the local `pc-server`.

Run:

```bash
cd web-app
npm install
npm run dev
```

Default server: `http://localhost:8787` (change in UI). API Key default: `dev-local-key`.

If running `pc-server` on another host, set the Server URL input accordingly.
